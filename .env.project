# Project configuration for py-chess

# Project name - controls naming of resources, Docker images, and 1Password vaults
# Format: <org>-<project>
PROJECT_NAME=krondor-chess

# Cloud provider vault in 1Password (shared across projects)
CLOUD_VAULT=cloud-providers

# Services to deploy
# Format: service_name:subdomain (relative to DNS_ROOT_ZONE)
# This app has a single service deployed to chess.krondor.org
SERVICES="chess:chess"

# DNS configuration - root zone managed in Cloudflare
DNS_ROOT_ZONE=krondor.org
